services:
  backend:
    volumes: []  # Disable code mounting in production, use copied files inside image
    restart: always

  ai:
    restart: always

  frontend:
    restart: always

  nginx:
    ports:
      - "80:80"
      - "443:443"
    restart: always
